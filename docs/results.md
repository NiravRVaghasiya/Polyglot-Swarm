# Measured results

> Generated by `polyglot results` (`python -m src.cli results`). Do not edit by hand — regenerate instead.

- Generated at: `2026-09-15T08:59:03.666755+00:00`
- Deterministic mode: `True`

These are actual numbers produced by running the project's measurement harnesses, not hand-written claims. See **Known limitations** at the bottom for exactly what they do and do not demonstrate.

## Component benchmarks

| Benchmark | Result | Duration (ms) | Key metrics |
|---|---|---|---|
| `provider_latency` | pass | 1.04 | reply_chars=37 |
| `structured_output_parseable` | pass | 0.63 | valid_json=True |
| `grammar_precision` | pass | 0.00 | precision=1.000, recall=1.000, false_correction_rate=0.000, cases=8 |
| `verifier_calibration` | pass | 0.00 | ece=0.000, buckets=3, predictions=30 |

## Evaluation suites

| Suite | Eval | Result | Key metrics |
|---|---|---|---|
| grammar | `precision_recall_f1` | pass | tp=5, fp=0, fn=0, precision=1.000, recall=1.000, f1=1.000, false_correction_rate=0.000, cases=17 |
| grammar | `calibration` | pass | ece=0.184, predictions=7 |
| vocabulary | `extraction_precision_recall_f1` | pass | tp=5, fp=0, fn=0, precision=1.000, recall=1.000, f1=1.000, cases=7 |
| vocabulary | `extraction_never_raises` | pass | cases=7, failures=0 |
| assessment | `correlation_and_absolute_error` | pass | correlation=1.000, band_mae=0.000, cases=13 |
| assessment | `calibration` | pass | ece=0.054, predictions=30 |
| assessment | `confidence_coverage` | pass | mean_confidence_well_evidenced=0.762, mean_confidence_low_evidence=0.175 |
| curriculum | `target_coverage` | pass | coverage=1.000, expected=7, covered=7, cases=4 |
| curriculum | `skill_balance` | pass | hit_rate=1.000, cases=3 |
| curriculum | `difficulty_appropriateness` | pass | accuracy=1.000, cases=5 |
| regression | `no_regression_gate` | pass | checked=7, failing=0 |

## Ablation study (which components create learning signal)

Experiment `ablation_ladder`, metric `mean_skill_mastery` (mean learner-model mastery over grammar + vocabulary, in [0, 1]). Each arm runs a scripted cohort through a different graph topology; the gain is post-session minus pre-session mastery.

| Arm | Description | n | pre | post | gain |
|---|---|---|---|---|---|
| A | conversation only | 3 | 0.000 | 0.000 | +0.000 |
| B | conversation + memory | 3 | 0.000 | 0.000 | +0.000 |
| C | conversation + FSRS review | 3 | 0.000 | 0.000 | +0.000 |
| D | conversation + analysis (learner model) | 3 | 0.000 | 0.333 | +0.333 |
| E | full system | 3 | 0.000 | 0.333 | +0.333 |

The arms without the analysis pipeline (A/B/C) accumulate no learner-model evidence and show zero gain; the arms with it (D/E) show a positive gain. That is the ablation's intended result: the learner-model analysis components are what turn a conversation into measurable evidence about the learner.

## Known limitations

- All numbers are produced in DETERMINISTIC mode (`POLYGLOT_DETERMINISTIC=1`): LLM calls resolve to a fake/scripted provider, not a live model. They measure the surrounding machinery, decision logic, and pipeline wiring — NOT live-model quality. Real-model precision/recall/latency will differ.
- The grammar false-correction-rate benchmark runs on a small, hand-labeled case set against the deterministic classification/abstention policy (`grammar._select_errors`), so it validates the policy, not end-to-end model behavior on a large corpus.
- The ablation study uses a scripted single-session 'lesson' and synthetic learners. It demonstrates that the analysis pipeline (arms D/E) produces measurable learner-model evidence that conversation-only arms (A/B/C) do not — a wiring/attribution result, not a claim about human learning gains.
- Delayed-retention measurement (1d/7d/30d) is wired end to end (`experiments.due_measurements` produces the due work list), but no longitudinal study with real learners over real calendar time has been run — so no delayed-retention numbers are reported here yet.
- CEFR estimates are model-internal, derived from the accumulated evidence; they have NOT been validated against expert human raters or an established language test. Correlation-with-experts numbers are therefore absent by design until such a study exists.
- Cost figures are estimates from a static per-model price table (`llm.telemetry.estimate_cost`), not billed amounts, and are $0 in deterministic mode since the fake provider is free.

## Reproduce

```bash
POLYGLOT_DETERMINISTIC=1 python -m src.cli results
# or, to print without writing the file:
POLYGLOT_DETERMINISTIC=1 python -m src.cli results --stdout
```
