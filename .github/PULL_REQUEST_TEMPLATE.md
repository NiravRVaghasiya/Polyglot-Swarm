## Summary

What does this PR change, and why? Link the issue it addresses, if any.

## What was tested

- [ ] `make check` (lint + format-check + typecheck + test) passes locally
- [ ] Added/updated tests for the behavior this PR changes
- [ ] Ran `make benchmark` / `make eval` if this touches LLM prompts, routing,
      the evaluator, or the learner model
- Manual testing notes (if any):

## Blocked / follow-up

Anything intentionally left out of scope, or follow-up work this PR creates.

## Checklist

- [ ] Existing test contracts are preserved (or the change/removal is
      explained above)
- [ ] `CHANGELOG.md` updated under `[Unreleased]` if this is user-visible
- [ ] An ADR added under `docs/adr/` if this is a structural/architectural
      decision (copy `docs/adr/template.md`)
- [ ] Docs under `docs/` updated if this changes documented behavior
- [ ] No secrets, API keys, or personal data included in this diff
